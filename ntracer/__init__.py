"""The names :py:class:`.render.Renderer`, :py:class:`.wrapper.NTracer`,
:py:data:`.wrapper.CUBE` and :py:data:`.wrapper.SPHERE` are imported here for
convenience."""


from ntracer.render import Renderer
from ntracer.wrapper import NTracer,CUBE,SPHERE

